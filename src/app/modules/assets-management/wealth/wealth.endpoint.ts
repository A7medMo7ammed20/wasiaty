import { APIUrlEndpoint } from "app/configs/api-url.interface";

export const WEALTH_ENDPOINTS:APIUrlEndpoint[] = [
    {
        mockEndpoint:'Asset',
        endpoint: 'Asset',
    },
    {
        mockEndpoint:'user',
        endpoint: 'user',
    },
];
