import { APIUrlEndpoint } from "app/configs/api-url.interface";

export const BEQUEST_ENDPOINTS:APIUrlEndpoint[] = [
    {
        mockEndpoint:'bequests',
        endpoint: 'bequests',
    },
    {
        mockEndpoint:'user',
        endpoint: 'user',
    },
];
